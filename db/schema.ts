import {sql} from "drizzle-orm";
import {integer,sqliteTable,text} from "drizzle-orm/sqlite-core";
export const bookings=sqliteTable("bookings",{id:integer("id").primaryKey({autoIncrement:true}),reference:text("reference").notNull().unique(),hostelName:text("hostel_name").notNull(),fullName:text("full_name").notNull(),phone:text("phone").notNull(),email:text("email"),roomType:text("room_type").notNull(),gender:text("gender").notNull(),moveInDate:text("move_in_date").notNull(),message:text("message"),status:text("status").notNull().default("pending"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const partnerLeads=sqliteTable("partner_leads",{id:integer("id").primaryKey({autoIncrement:true}),hostelName:text("hostel_name").notNull(),managerName:text("manager_name").notNull(),phone:text("phone").notNull(),plan:text("plan").notNull(),rooms:text("rooms"),status:text("status").notNull().default("new"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const hostelEdits=sqliteTable("hostel_edits",{id:integer("id").primaryKey({autoIncrement:true}),name:text("name").notNull().unique(),payload:text("payload").notNull().default("{}"),status:text("status").notNull().default("active"),updatedBy:text("updated_by").notNull(),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const managerAccounts=sqliteTable("manager_accounts",{id:integer("id").primaryKey({autoIncrement:true}),username:text("username").notNull().unique(),displayName:text("display_name").notNull(),hostelName:text("hostel_name"),role:text("role").notNull().default("manager"),passwordHash:text("password_hash").notNull(),passwordSalt:text("password_salt").notNull(),active:integer("active").notNull().default(1),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
